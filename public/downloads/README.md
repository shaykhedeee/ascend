# APK Downloads

This directory hosts the signed APK files served at `/downloads/resurgo-latest.apk`.

The APK is produced by `npm run android:build:release` and automatically copied here.

**Important:** APK files are `.gitignore`d — the build pipeline generates them.
